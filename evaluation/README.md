## Evaluation and Metrics

This folder contains scripts for evaluating trained models.

- `evaluation.py`: Runs YOLO validation on trained weights.
- `metrics.py`: Computes precision, recall, and mAP statistics.
- `charts.py`: Generates training and evaluation plots.

These scripts are used after training to compare model performance.

